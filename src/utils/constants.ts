export const APP_NAME = '1social';

export const ACCESS_TOKEN = 'access_token';

export const CONFIG = 'config';

export const REGEX = {
  username: /^(?![_.])[a-zA-Z0-9._]+(?<![_.])$/,
  keyword: /^(?![_-])[a-zA-Z0-9-]+(?<![_-])$/,
};
