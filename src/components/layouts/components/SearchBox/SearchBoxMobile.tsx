import { SearchRounded } from '@mui/icons-material';
import { IconButton } from '@mui/material';

export function SearchBoxMobile() {
  return (
    <IconButton sx={{ bgcolor: 'action.hover' }} onClick={() => {}}>
      <SearchRounded />
    </IconButton>
  );
}
